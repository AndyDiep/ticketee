<h2><%= @ticket.title %></h2>
<%= link_to "Edit", [:edit, @project, @ticket] %>
<%= link_to "Delete", [@project, @ticket], :method => :delete, :confirm => "Are your sure you want to delete?" %>
<%= simple_format(@ticket.description) %>
